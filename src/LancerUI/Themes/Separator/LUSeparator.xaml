<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:LancerUI.Controls.Separator">
    <Style TargetType="{x:Type local:LUSeparator}">
        <Setter Property="Focusable"
                Value="false"/>
        <Setter Property="Margin"
                Value="0,2,0,2"/>
        <Setter Property="Background" Value="{DynamicResource LUSeparatorBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:LUSeparator}">
                    <Border Background="{TemplateBinding Background}" Height="1" SnapsToDevicePixels="True"/>
                    <!--<Rectangle Fill="Black" Height="1" Width="{TemplateBinding Width}"/>-->
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>